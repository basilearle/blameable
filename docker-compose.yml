services:
  blameable-portal-ui:
    container_name: blameable-portal-ui
    image: blameable/portal-ui
    depends_on:
      - blameable-core-api
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - API_URL=blameable-core-api:3000
      - API_SITE_ID=9d607f41-c697-4af1-b8b1-65b514763aee
  blameable-admin-ui:
    container_name: blameable-admin-ui
    image: blameable/admin-ui
    depends_on:
      - blameable-core-api
    restart: unless-stopped
    ports:
      - "8081:80"
    environment:
      - API_URL=blameable-core-api:3000
  blameable-core-api:
    container_name: blameable-core-api
    image: blameable/core-api
    depends_on:
      - blameable-core-db
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.docker-compose
  blameable-core-db:
    container_name: blameable-core-db
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    env_file:
      - .env.docker-compose
    volumes:
      - ./libs/core-data/pg_data:/var/lib/postgresql/data
      - ./libs/core-data/init.sql:/docker-entrypoint-initdb.d/init.sql
  blameable-storybook:
    container_name: blameable-storybook
    image: blameable/storybook
    restart: unless-stopped
    ports:
      - "4400:4400"
      - "4401:4401"
      - "4402:4402"
      - "4403:4403"
